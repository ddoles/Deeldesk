[build]
builder = "nixpacks"

[deploy]
# Healthcheck disabled temporarily for initial deployment
# healthcheckPath = "/api/health"
# healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
